{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build && cp -r dist /tmp/dist-cache"
  },
  "deploy": {
    "startCommand": "cp -r /tmp/dist-cache/* dist/ 2>/dev/null; node server.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
