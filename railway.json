{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build && mkdir -p /tmp/dist-cache && cp -r dist/* /tmp/dist-cache/"
  },
  "deploy": {
    "startCommand": "mkdir -p dist && cp -r /tmp/dist-cache/* dist/ && node server.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
